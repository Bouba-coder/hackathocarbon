// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// shemat User -- on change le role users pour que le rh soit rh
model User {
  id         Int          @id @default(autoincrement())
  email      String       @unique
  firstName  String
  lastName   String
  password   String
  role       Role         @default(CONSULTANT)
  consultant Consultant? 
  formation  Formation[]
  createdAt  DateTime     @default(now())
  updatedAt  DateTime     @updatedAt
<<<<<<< HEAD
=======
  Consultant Consultant[]
  Article    Article[]
  Formation  Formation[]
}

enum Disponibilite {
  IMMEDIAT
  TROIS_MOIS
  SIX_MOIS
>>>>>>> dev
}

enum Role {
  CONSULTANT
  ADMIN
  RH
}

model Entreprise {
  id         Int          @id @default(autoincrement())
<<<<<<< HEAD
  nom        String       @unique
  contact    String?      
=======
  contact    String
  nom        String
  secteur    String
  // relation avec le model Consultant
>>>>>>> dev
  consultant Consultant[]
  createdAt  DateTime     @default(now())
  updatedAt  DateTime     @updatedAt
}

<<<<<<< HEAD
model Formation {
  id          Int          @id @default(autoincrement())
  nom         String       @unique
  niveau      String?
  description String?
  duree       Int?
  users       User[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
=======
// shemat Experience
// model Experience {
//   id             Int          @id @default(autoincrement())
//   nom_entreprise String
//   poste          String
//   // relation avec le model Consultant
//   consultant     Consultant[]
// }

// shemat Formation
model Formation {
  id      Int    @id @default(autoincrement())
  nom     String
  diplome String
  niveau  String
  // relation avec le model Consultant
  //consultant Consultant[]
  //relation with user with role rh
  user    User   @relation(fields: [userId], references: [id])
  userId  Int
>>>>>>> dev
}

model Consultant {
<<<<<<< HEAD
  id            Int           @id @default(autoincrement())
  metier        String?
  secteur       String?
  disponibilite String?
  perimetre     String?
  competences   String[]
  level         Int?
  salaire       Int?
  experiences   String[]
  parcours      String[]
  entreprise   Entreprise?    @relation(fields: [entrepriseId], references: [id])
  entrepriseId  Int?
  user          User          @relation(fields: [userId], references: [id])
  userId        Int           @unique
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}
=======
  id                  Int           @id @default(autoincrement())
  email               String        @unique
  role                Role          @default(CONSULTANT)
  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt
  metier              String
  secteur             String
  disponibilite       Disponibilite
  perimetre           String
  competences         String[]
  experiences         String[]
  formations          String[]
  //clients relation
  actuel_entreprise   Client        @relation(fields: [actuel_entrepriseId], references: [id])
  actuel_entrepriseId Int
  level               Int
  salaire             Int
  // relation avec le model User
  user                User          @relation(fields: [userId], references: [id])
  userId              Int
}

// shemat article
model Article {
  id       Int    @id @default(autoincrement())
  title    String
  content  String
  category String
  author   User   @relation(fields: [authorId], references: [id])
  authorId Int // relation scalar field  (used in the `@relation` attribute above)
}
>>>>>>> dev
